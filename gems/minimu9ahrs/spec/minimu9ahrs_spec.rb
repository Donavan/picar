require 'spec_helper'

describe Minimu9ahrs do
  it 'has a version number' do
    expect(Minimu9ahrs::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
